# ESP32 Motion Detector - Docker Environment Configuration
# =========================================================

# Server Configuration
# --------------------

# Port for web interface
SERVER_PORT=5000

# Authentication token (REQUIRED - must match ESP32/client config)
# Generate with: openssl rand -base64 32
AUTH_TOKEN=YOUR_SECRET_TOKEN_CHANGE_ME_12345

# Notification Backend
# Options: disabled, linux_notify, windows_toast
# Note: Toast notifications don't work in Docker (use 'disabled')
NOTIFICATION_BACKEND=disabled

# Face Recognition
# ----------------

# Enable face recognition (true/false)
FACE_RECOGNITION_ENABLED=true

# Optional: Adminer Database UI
# ------------------------------

# Port for database admin interface (only with --profile admin)
ADMINER_PORT=8080

# Advanced: Server Bind Address
# ------------------------------

# Usually don't change this
# SERVER_HOST=0.0.0.0
