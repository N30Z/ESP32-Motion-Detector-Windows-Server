/*
 * ESP32 Motion Detector - Secrets Configuration
 * ==============================================
 *
 * INSTRUCTIONS:
 * 1. Copy this file to: include/secrets.h
 * 2. Fill in your WiFi credentials and server details
 * 3. NEVER commit secrets.h to version control!
 *
 * Note: secrets.h is in .gitignore
 */

#ifndef SECRETS_H
#define SECRETS_H

// ============================================================================
// WIFI CONFIGURATION
// ============================================================================

// Your WiFi network name
#define WIFI_SSID "YourWiFiNetworkName"

// Your WiFi password
#define WIFI_PASSWORD "YourWiFiPassword"

// WiFi connection timeout (milliseconds)
#define WIFI_TIMEOUT_MS 20000

// ============================================================================
// SERVER CONFIGURATION
// ============================================================================

// Server IP address (Windows PC running Flask server)
// Example: "192.168.1.100" or "10.0.0.50"
#define SERVER_HOST "192.168.1.100"

// Server port (must match config.yaml)
#define SERVER_PORT 5000

// Authentication token (must match config.yaml)
// Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
#define AUTH_TOKEN "YOUR_SECRET_TOKEN_CHANGE_ME_12345"

// ============================================================================
// DEVICE CONFIGURATION
// ============================================================================

// Device identifier (appears in filenames and logs)
#define DEVICE_ID "ESP32-CAM-01"

#endif // SECRETS_H
